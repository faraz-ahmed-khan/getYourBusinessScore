import { Hero } from '@/components/home/Hero';

/**
 * GYBS Homepage — Level 1 entry only.
 * Must NOT include: readiness cards, pathways, opportunities, training content, educational content.
 * Kiosk-style: hero + primary CTA only.
 */
export default function HomePage() {
  return (
    <div>
      <Hero />
    </div>
  );
}
